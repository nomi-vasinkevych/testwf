name: Test

on:
  workflow_dispatch:
    inputs:
      who:
        description: 'Who?'
        required: true
        default: 'fred'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Attach PDF
      if: contains('["kingthorin","lmnopxyzabc"]', github.actor)
      run: echo {{ github.event.inputs.who }}
